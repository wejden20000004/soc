# Copy to .env and fill in real values. .env is git-ignored.

# Elasticsearch (compose.siem.yml)
ES_PASSWORD=changeme

# MISP (compose.misp.yml) — fill in once compose.misp.yml is fleshed out
# MYSQL_ROOT_PASSWORD=
# MISP_ADMIN_EMAIL=

# TheHive (compose.thehive.yml) — fill in once compose.thehive.yml is fleshed out
# THEHIVE_SECRET=
