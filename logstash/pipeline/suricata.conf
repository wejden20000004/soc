# TODO: filters for Suricata eve.json fields shipped by native Filebeat over 5044.
# Skeleton only — fill in the filter block once field mapping is decided.
input {
  beats {
    port => 5044
  }
}

filter {
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    user => "elastic"
    password => "${ES_PASSWORD}"
    index => "suricata-%{+YYYY.MM.dd}"
  }
}
