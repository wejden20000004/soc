Native Windows Suricata service config (Phase 2b).

Put the working `suricata.yaml` here for version control once it's tuned (capture interface
set to the VMnet8 adapter, Emerging Threats Open ruleset enabled). The live copy runs from
`C:\Program Files\Suricata\` on the host; treat this folder as the checked-in backup/reference,
not the path Suricata actually reads from.
