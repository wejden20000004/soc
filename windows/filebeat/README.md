Native Windows Filebeat service config (Phase 2b).

Put the working `filebeat.yml` here (Suricata module enabled, output to `localhost:5044`)
for version control once it's set up. The live copy runs from the Filebeat install directory
on the host; this folder is the checked-in backup/reference, not the live path.
